# Copy to proxy/.env and fill in the values printed by scripts/subscribe.ts.
# proxy/.env is gitignored. Never commit real tokens.

TXLINE_JWT=<paste the eyJ... JWT here>
TXLINE_API_TOKEN=<paste the txoracle_api_... token here>

# Optional, these have sensible defaults.
TXLINE_BASE=https://txline-dev.txodds.com
PORT=8080
